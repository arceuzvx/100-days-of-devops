Single vs Multi container & brushing up on yaml 

Run containers with the following cmds: 

```bash

docker compose -f docker-compose-simple.yml up

docker compose -f docker-compose-multi.yml up -d
docker ps
`